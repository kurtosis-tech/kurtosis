package engine_labels_schema

const (
	// TODO Unify this, plus the labels in the API container, into a separate "Kurtosis labels schema" library!!
	labelPrefix = "com.kurtosistech."

	// TODO These are duplicated from the API container!!!!!!
	AppIDLabel = "app-id"
	AppIDLabelValue = "kurtosis"

	// TODO This needs to be centralized into a labels library!
	ContainerTypeKurtosisEngine = "kurtosis-engine"
)
