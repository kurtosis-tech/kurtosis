import { EnclaveFullInfo } from "../../types";

type EnclaveSourceProps = { enclave: EnclaveFullInfo };

export const EnclaveSource = ({ enclave }: EnclaveSourceProps) => {
  return <div>Placeholds</div>;
};
