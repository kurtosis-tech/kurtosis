# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2.1
orbs:
  go: circleci/go@1.3.1
jobs:
  build:
    machine: {}  # Leave out the image, to pull in the latest
    steps:
      - checkout
        # - go/load-cache
        # - go/mod-download
        # - go/save-cache
      - run: ls
      - run: pwd
      - run: go version

      # TODO Add caching like in https://circleci.com/docs/2.0/language-go/
      #- if [ "$TRAVIS_PULL_REQUEST" != "false" ]; then bash .ci/check_for_changelog_in_diff.sh; fi
      - run: scripts/full_build_and_run.sh
